spring:
  datasource:
    url: jdbc:mysql://localhost/local1?autoReconnect=true&useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
      show_sql: true

server:
  port: 8081
  servlet:
    context-path: /auth

auth:
  salt: VLXMGMAUTHDEV
  sign: Velox
  tokenTime: 3600000