spring:
  datasource:
    url: jdbc:mysql://localhost/local?autoReconnect=true&useSSL=false
    username: local
    password: local
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

server:
  port: 8081
  servlet:
    context-path: /auth

auth:
  salt: VLXMGMAUTHDEV
  sign: Velox
  tokenTime: 3600000
  forgot-password.expired-in-minutes: 60